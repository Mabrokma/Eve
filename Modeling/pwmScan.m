function [sites, scores] = pwmScan(locus, pwm)
%Scans a locus (or entire genome) and scores each frame, sliding 1 base at
%a time, for each of the pwms provided; the return is a struct with
%locations and lod scores of each binding site identified as well as the
%complete scoring by each pwm at each frame


end